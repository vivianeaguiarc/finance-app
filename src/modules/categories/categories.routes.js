import { Router } from 'express'

/**
 * CRUD de categorias ainda não implementado.
 * O módulo existe como placeholder para evolução futura da arquitetura modular.
 */
export const categoriesRouter = Router()
